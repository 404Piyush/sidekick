<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      M1 talks to a local Ollama over plain HTTP. We only permit cleartext
      for the emulator's host-loopback alias (`10.0.2.2`) and the localhost
      name. Every other host must speak TLS. M2 can revisit if the user
      wants to point at a remote LAN Ollama over plain HTTP — for now the
      assumption is "Ollama runs on the dev machine".
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>