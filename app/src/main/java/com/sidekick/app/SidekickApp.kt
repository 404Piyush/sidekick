package com.sidekick.app

/**
 * Application root. The Activity is the runtime owner; this is the conceptual
 * Sidekick application — a place to host DI / lifecycle-scoped singletons in
 * later milestones. For M0 the class is intentionally empty.
 */
object SidekickApp {
    const val NAME: String = "Sidekick"
    const val VERSION: String = "0.1.0"
}
